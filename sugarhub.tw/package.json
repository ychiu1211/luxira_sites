{
  "name": "sugarhub-tw",
  "version": "1.0.0",
  "description": "Static website for sugarhub.tw",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=sugarhub-tw --branch=main"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
